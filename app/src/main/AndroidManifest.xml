<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="tech.the_code_builder.golazo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Login"></activity>

        <activity android:name=".Signup"></activity>

        <activity android:name=".Home"></activity>

        <activity android:name=".About"></activity>

        <activity android:name=".News"></activity>

        <activity android:name=".Leader"></activity>

        <activity android:name=".Schedule"></activity>

        <activity android:name=".Point"></activity>

        <activity android:name=".Team"></activity>

        <activity android:name=".UserData"></activity>

        <activity android:name=".Profile"></activity>

        <activity android:name=".CreateTeam"></activity>

        <activity android:name=".Confirm"></activity>

    </application>

</manifest>